<%- include ('partials/header.ejs') %>

    <body>
        <%- include('partials/navbar.ejs') %>
            <div class="container">
                <hr>
                <h1>Perguntas</h1>

                <a href="/pergunta" class="btn btn-primary"> Faça uma pergunta</a>
                <hr>
                <br>
                <% perguntas.forEach(questionPage=> {%>
                    <br>
                    <div class="card">
                        <div class="card-body">
                            <h3>
                                <%= questionPage.title %>
                            </h3>
                        </div>
                        <div class="card-footer">
                            <a href="/questionPage/<%= questionPage.id %>"><button class="btn btn-primary">Responder</button></a>
                            </a>
                        </div>
                    </div>
                    <% }) %>
            </div>
    </body>
    <%- include ('partials/footer.ejs') %>